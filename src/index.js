/* eslint-disable import/default */

import React from 'react';
import { render } from 'react-dom';
// import { Router, browserHistory } from 'react-router';


render(
    <h1> HEllow from react </h1>, document.getElementById('app')
);
